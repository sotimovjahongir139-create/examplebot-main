version: "3.9"

services:
  bot:
    image: python:3.12-slim
    working_dir: /app
    command: sh -c "pip install -r requirements.txt && python main.py"
    env_file:
      - .env
    volumes:
      - ./:/app

